export const getCategories = (state) => state.collection.categories.data;

export const getSubcategories = (state) => state.collection.subcategories.data;

export const getProducts = (state) => state.collection.products.data;

export const getProductsdb = (state) => state.collection.productsdb.data;

export const getCategory = (state) => state.collection.category;

export const getSubcategory = (state) => state.collection.subcategory;
